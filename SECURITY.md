# Security policy

Security fixes are applied to the latest release line. Please do not include
private manga pages, model cache paths, or machine details in a public issue.
Report vulnerabilities through GitHub's private security advisory feature.

The server binds to `127.0.0.1` by default and is not designed to be exposed
directly to the public internet.
